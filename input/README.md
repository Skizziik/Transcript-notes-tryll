# input/

Кидай сюда аудиофайл (m4a, mp3, wav, mp4, mov, opus, aac, flac, aiff, ogg, webm).

Запусти `/notes` в Claude Code — он сам подхватит файл, транскрибирует и сделает заметки в `../output/`.

Если положишь несколько файлов — `/notes` попросит уточнить какой обрабатывать.
